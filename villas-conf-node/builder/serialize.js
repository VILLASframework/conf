/**
 * Deserialize a json incoming message resulting in a builder.
 *
 * @param {Object} jsonMsg - The incoming json message
 * @param {Array} jsonMsg.nodes - The nodes already configured
 * @param {Array} jsonMsg.paths - The paths already configured
 */
function deserialize(jsonMsg) {
  // This would
  const { nodes, paths } = jsonMsg;
}
